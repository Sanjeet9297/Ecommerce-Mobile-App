import React from 'react';
import SettingsPage from '../components/setting/page';

/**
 * Settings Route
 * Handles global application preferences and user account settings.
 */
export default function SettingsRoute() {
  return <SettingsPage />;
}
